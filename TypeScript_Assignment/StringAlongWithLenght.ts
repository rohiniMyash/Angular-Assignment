
     let word :String[];
     word=["shubham","sumit","tushar"];
    //  let lengths = word.map((string) => string.length);
  
     let l=word.forEach(s=>console.log(" lenght "+s.length+" string name "+s));

    
