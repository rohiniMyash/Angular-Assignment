var word;
word = ["shubham", "sumit", "tushar"];
//  let lengths = word.map((string) => string.length);
var l = word.forEach(function (s) { return console.log(" lenght " + s.length + " string name " + s); });
